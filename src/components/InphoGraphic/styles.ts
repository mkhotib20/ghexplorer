import { css } from '@emotion/react';

export const cssInfoGraphicWrapper = css({
  height: '100%',
  width: '100%',
  textAlign: 'center',
  minHeight: 500,
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
});
